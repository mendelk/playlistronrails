<h1>Listing songs</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @songs.each do |song| %>
  <tr>
    <td><%= song.name %></td>
    <td><%= link_to 'Show', [@artist, song] %></td>
    <td><%= link_to 'Edit', edit_artist_song_path(@artist, song) %></td>
    <td><%= link_to 'Destroy', [@artist, song], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>
